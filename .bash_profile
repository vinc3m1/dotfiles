# Login shells: behave like non-login interactive shells.
# Real config lives in ~/.bashrc so zellij and Linux terminals (which start
# non-login shells) get the same setup.
[[ -f ~/.bashrc ]] && . ~/.bashrc
